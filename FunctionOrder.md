handleClickMoveBtn(move) {

setAttackOrder(move) {
    
}

executeTurn(fastPokemon, slowPokemon) {
    pokemonIsAttacked(atkPkm, defPkm) {
        ifEffectRunScript(effect)

        if (iMoveFirst) {
            setEnemyHP(prevHP => {
                return prevHP - dmg > 0 ? prevHP - dmg : 0
            }) 
        } else 
    }
}
	
	
	
	
}